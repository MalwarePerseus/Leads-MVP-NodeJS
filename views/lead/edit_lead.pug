doctype html
html(lang='en')
  include ../common/head.pug
  +head("Leads Management | Node & Express")
  body
    form(action="/lead/" + lead.id + "/edit", method="POST")
      input(type="email", name="lead_email", placeholder="example@domain.com", value=lead.email)
      button(type="submit") Save